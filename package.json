{
  "name": "ai-digest-tg",
  "private": true,
  "workspaces": ["worker"],
  "scripts": {
    "dev": "cd worker && wrangler dev",
    "deploy": "cd worker && wrangler deploy",
    "test:worker": "cd worker && vitest run",
    "test:scripts": "cd scripts && uv run pytest"
  }
}
